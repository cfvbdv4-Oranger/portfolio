# Portfolio Project Instructions

Before working on this project, read `WORKFLOW_NOTES.md` in this directory and follow its low-token workflow.

Key rules:

- This is the active project: `E:\燕云\portfolio-publish`.
- Do not use the older `portfolio-site` directory.
- Preserve existing local changes and do not commit preview images or `desktop.ini`.
- Use `apply_patch` for text and CSS edits.
- New visible text must include matching `data-zh` and `data-en` translations.
- Force `.reveal` elements visible before automated screenshots.
- Do not deploy unless the user explicitly approves it.
- For GitHub pushes, use the known working proxy/OpenSSL command documented in `WORKFLOW_NOTES.md`.
