const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting)entry.target.classList.add("visible")})},{threshold:.08});document.querySelectorAll(".project, .section-heading, .capability-grid article, .intro-strip > *").forEach(element=>{element.classList.add("reveal");observer.observe(element)});
const hero=document.querySelector(".hero");if(hero){hero.addEventListener("pointermove",event=>{const box=hero.getBoundingClientRect();const x=(event.clientX-box.left)/box.width-.5;const y=(event.clientY-box.top)/box.height-.5;hero.style.setProperty("--mx",`${x*14}px`);hero.style.setProperty("--my",`${y*10}px`)});hero.addEventListener("pointerleave",()=>{hero.style.setProperty("--mx","0px");hero.style.setProperty("--my","0px")});addEventListener("scroll",()=>{hero.style.setProperty("--scroll",`${Math.min(scrollY*.1,45)}px`)},{passive:true})}
const languageToggle=document.querySelector(".language-toggle");if(languageToggle){let language="zh";languageToggle.addEventListener("click",()=>{language=language==="zh"?"en":"zh";document.documentElement.lang=language==="zh"?"zh-CN":"en";document.querySelectorAll("[data-zh][data-en]").forEach(element=>{element.innerHTML=element.dataset[language]});languageToggle.classList.toggle("is-en",language==="en")})}
